/* Optional server-side booking-code resolver endpoint. Keep empty until a
   trusted resolver service is configured. Never put provider secrets here. */
window.PredictIQConfig={betCodeResolverUrl:''};