/* PredictIQ Daily Picks — verified fixtures + cached recent-form scoring. */
(function(){
'use strict';
const state={fixtures:[],picks:[],updatedAt:null,source:null,error:null};
const $=s=>document.querySelector(s);
const esc=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
function todayISO(){return new Intl.DateTimeFormat('en-CA',{year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());}
function formScore(x){if(!x?.rows?.length)return null;const r=x.rows,pts=r.reduce((s,m)=>s+(m.r==='W'?3:m.r==='D'?1:0),0)/(r.length*3),gd=r.reduce((s,m)=>s+(m.gf-m.ga),0)/Math.max(1,r.length);return Math.max(0,Math.min(1,.55*pts+.45*(.5+gd/6)));}
function makePick(f,h,a){const hs=formScore(h),as=formScore(a);if(hs==null||as==null)return {fixture:f,markets:[],confidence:0,missingForm:true};const edge=hs-as,home=Math.max(.05,Math.min(.9,.5+edge*.38+.08)),draw=Math.max(.05,Math.min(.45,.25-Math.abs(edge)*.08)),away=Math.max(.05,1-home-draw),total=hs+as,over25=Math.max(.12,Math.min(.88,.18+total*.55)),btts=Math.max(.12,Math.min(.88,.25+total*.42));const markets=[{name:'Home or Draw (1X)',prob:home+draw,why:'Home side has the stronger recent-form signal.'},{name:'Away or Draw (X2)',prob:away+draw,why:'Away side has the stronger recent-form signal.'},{name:'Under 4.5 Goals',prob:1-Math.max(.03,over25*.55),why:'Recent scoring and goal-difference signals do not support an extreme total.'},{name:'Both Teams To Score',prob:btts,why:'Recent attacking output on both sides supports a scoring scenario.'}].filter(m=>m.prob>=.80).sort((a,b)=>b.prob-a.prob).slice(0,3);return {fixture:f,markets,confidence:markets[0]?.prob||0,homeForm:hs,awayForm:as,missingForm:false,formMatches:Math.min(h.rows.length,a.rows.length)};}
function fixtureCard(f,p,i){const kickoff=f.kickoff?new Date(f.kickoff):null;const when=kickoff&&!Number.isNaN(kickoff.getTime())?kickoff.toLocaleString():((f.kickoffLocal||'')+' '+(f.timeLocal||'')).trim()||'Time unavailable';const status=String(f.status||'NS').toUpperCase();if(p?.markets?.length)return `<article class="pick-item"><div class="rank">#${i+1}</div><div><h4>${esc(f.home)} vs ${esc(f.away)}</h4><p>${esc(f.competition||'Football')} • ${esc(when)} • ${esc(status)}</p>${p.markets.map(m=>`<p><strong>${esc(m.name)}</strong> — ${(m.prob*100).toFixed(1)}%<br><small>${esc(m.why)}</small></p>`).join('')}<p><span class="pick-label">MODEL QUALIFIED</span> • Recent form: ${p.formMatches} matches/team</p></div></article>`;return `<article class="pick-item"><div class="rank">${i+1}</div><div><h4>${esc(f.home)} vs ${esc(f.away)}</h4><p>${esc(f.competition||'Football')} • ${esc(when)} • ${esc(status)}</p><p><strong>${p?.error?'Analysis delayed':'Analysis completed — no qualifying 80% market'}</strong> — ${p?.missingForm?'the free providers did not return enough recent-form data for both teams.':'the verified data did not meet the model threshold.'}</p></div></article>`;}
function render(){const host=$('#dailyList')||$('#daily-picks-list')||$('#picks-list')||$('.picks-list'),count=$('#dailyCount');const qualified=state.picks.filter(p=>p.markets.length).length,analyzed=state.picks.filter(p=>!p.missingForm&&!p.error).length;if(count)count.textContent=state.fixtures.length?`${state.fixtures.length} verified fixtures • ${analyzed} analyzed • ${qualified} qualifying picks`:'No verified fixtures found';if(!host)return;if(state.error){host.innerHTML='<div class="panel"><strong>Daily Picks unavailable</strong><p>'+esc(state.error)+'</p></div>';return;}if(!state.fixtures.length){host.innerHTML='<div class="panel"><strong>No verified upcoming fixtures found</strong><p>The live providers returned no upcoming football matches. The system will not invent matches.</p></div>';return;}const byId=new Map(state.picks.map(p=>[p.fixture.id,p]));host.innerHTML=state.fixtures.slice(0,50).map((f,i)=>fixtureCard(f,byId.get(f.id),i)).join('');}
async function getRecentForm(api,teamId,teamName,cache){const key=(teamId||'name:'+String(teamName||'').toLowerCase()).trim();if(cache.has(key))return cache.get(key);const task=(async()=>{if(teamId&&api?.recentMatchesById){try{const r=await api.recentMatchesById(teamId,teamName,5);if(r?.rows?.length)return r;}catch(_){} }if(teamName&&api?.recentMatches){try{return await api.recentMatches(teamName,5);}catch(_){}}return null;})();cache.set(key,task);return task;}
async function load(){state.error=null;state.fixtures=[];state.picks=[];try{const r=window.PredictIQDataProvider?.getDailyFixtures?await window.PredictIQDataProvider.getDailyFixtures(todayISO()):window.PredictIQFreeData?.getDailyFixtures?await window.PredictIQFreeData.getDailyFixtures(todayISO()):null;if(!r)throw new Error('No daily fixture provider is connected.');state.fixtures=Array.isArray(r.fixtures)?r.fixtures:[];state.source=r.source||'free provider';const api=window.PredictIQFreeData;if(!api?.recentMatchesById&&!api?.recentMatches)throw new Error('Recent-form lookup is not connected.');const cache=new Map();const teams=[...new Map(state.fixtures.flatMap(f=>[[f.homeTeamId||'name:'+f.home,f.homeTeamId?{id:f.homeTeamId,name:f.home}:{id:'',name:f.home}],[f.awayTeamId||'name:'+f.away,f.awayTeamId?{id:f.awayTeamId,name:f.away}:{id:'',name:f.away}]]).map(([k,v])=>[k,v])).values()];const forms=new Map();for(let i=0;i<teams.length;i+=8){const batch=teams.slice(i,i+8);const vals=await Promise.all(batch.map(t=>getRecentForm(api,t.id,t.name,cache)));batch.forEach((t,j)=>forms.set(t.id||'name:'+t.name,vals[j]));}state.picks=state.fixtures.slice(0,50).map(f=>{const h=forms.get(f.homeTeamId||'name:'+f.home),a=forms.get(f.awayTeamId||'name:'+f.away);return makePick(f,h,a);});state.updatedAt=new Date().toISOString();}catch(e){state.error=e?.message||'Unable to load verified daily picks.';}render();const stamp=$('#dailyUpdated');if(stamp&&!state.error)stamp.textContent='Last verified analysis: '+new Date(state.updatedAt).toLocaleString()+' • '+(state.source||'provider');}
window.PredictIQDailyPicks={load,refresh:load,getState:()=>({...state})};document.addEventListener('DOMContentLoaded',load);
})();
